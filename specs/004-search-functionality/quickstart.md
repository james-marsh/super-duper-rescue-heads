# Quickstart: Search Functionality

1. Add Azure.Search.Documents NuGet package
2. Create search index schema
3. Implement ISearchService with Azure SDK
4. Add domain event handlers for indexing
5. Add GET /api/v1/search endpoint
6. Create search UI with filters

**Key Code**: SearchService.SearchAsync(), IndexItemAsync(), SearchEndpoints.cs, Search/Index.razor
